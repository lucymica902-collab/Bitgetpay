<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Bitget</title>
    <style>
        body { font-family: sans-serif; background: #121212; color: white; margin: 0; padding: 20px; }
        .header { display: flex; justify-content: space-between; align-items: center; background: #1f1f1f; padding: 15px 20px; border-radius: 8px; border: 1px solid #333; }
        .card { background: #1f1f1f; margin-top: 20px; padding: 20px; border-radius: 8px; border: 1px solid #333; }
        input { padding: 10px; margin: 5px 0 12px 0; width: 100%; border: 1px solid #444; border-radius: 6px; box-sizing: border-box; background: #2a2a2a; color: white; }
        button { padding: 10px 15px; background: #00ffcc; color: black; border: none; border-radius: 6px; font-weight: bold; cursor: pointer; }
        .reject-btn { background: #ff4d4d; color: white; }
        table { width: 100%; border-collapse: collapse; margin-top: 15px; }
        th, td { border: 1px solid #333; padding: 10px; text-align: left; font-size: 13px; }
        th { background: #2a2a2a; color: #f0b90b; }
        img { width: 50px; height: 50px; object-fit: cover; border-radius: 4px; }
        .logout { background: #ff4d4d; color: white; padding: 8px 15px; text-decoration: none; border-radius: 6px; font-size: 14px; font-weight: bold; }
    </style>
</head>
<body>

    <div class="header">
        <h2>Admin Dashboard</h2>
        <a href="/admin-logout" class="logout">Logout</a>
    </div>

    <!-- Settings, Support Link & VIP Control Card -->
    <div class="card">
        <h3>Control Deposit Settings, Support Link & VIP Levels</h3>
        <form action="/admin/settings" method="POST">
            <label><b>TRC20 Wallet Address</b></label>
            <input type="text" name="trc_address" value="<%= settings.trc_address %>" required>

            <label><b>USDT to INR Rate</b></label>
            <input type="text" name="usdt_rate" value="<%= settings.usdt_rate %>" required>

            <label><b>Telegram Customer Support Link</b></label>
            <input type="text" name="support_link" value="<%= settings.support_link %>" required>

            <h3 style="margin-top: 25px; color: #f0b90b; border-top: 1px solid #333; padding-top: 15px;">Manage VIP Levels (Price, Daily Return, Days)</h3>
            <% settings.vip_levels.forEach((vip) => { %>
                <div style="background: #2a2a2a; padding: 15px; margin-bottom: 12px; border-radius: 8px; border: 1px solid #444;">
                    <b style="color: #f0b90b; font-size: 15px;"><%= vip.level %></b><br><br>
                    <label>Price (USDT):</label>
                    <input type="text" name="v_price" value="<%= vip.price %>" required>
                    
                    <label>Daily Return (USDT):</label>
                    <input type="text" name="v_daily" value="<%= vip.daily %>" required>
                    
                    <label>Time Period (Days):</label>
                    <input type="text" name="v_days" value="<%= vip.days %>" required>
                </div>
            <% }) %>

            <button type="submit" style="width: 100%; padding: 14px; background: #f0b90b; color: black; font-size: 16px; margin-top: 10px;">Save All Settings & VIP Plans</button>
        </form>
    </div>

    <!-- Transactions Control Card -->
    <div class="card">
        <h3>User Deposits & Verification</h3>
        <table>
            <tr>
                <th>Date</th>
                <th>User Phone</th>
                <th>Amount</th>
                <th>TXID</th>
                <th>Screenshot</th>
                <th>Status</th>
                <th>Action</th>
            </tr>
            <% if(transactions.length === 0) { %>
                <tr><td colspan="7" style="text-align: center; color: #aaa;">No transactions found.</td></tr>
            <% } else { %>
                <% transactions.forEach(tx => { %>
                    <tr>
                        <td><%= tx.date %></td><td><%= tx.phone %></td>
                        <td><b><%= tx.amount %> USDT</b></td>
                        <td><%= tx.txid %></td>
                        <td>
                            <% if(tx.screenshot) { %>
                                <a href="/uploads/<%= tx.screenshot %>" target="_blank"><img src="/uploads/<%= tx.screenshot %>"></a>
                            <% } else { %> No Image <% } %>
                        </td>
                        <td><b><%= tx.status %></b></td>
                        <td>
                            <% if(tx.status === 'Pending') { %>
                                <form action="/admin/verify/<%= tx.id %>" method="POST" style="display:inline;">
                                    <button type="submit">Approve</button>
                                </form>
                                <form action="/admin/reject/<%= tx.id %>" method="POST" style="display:inline; margin-left: 5px;">
                                    <button type="submit" class="reject-btn">Reject</button>
                                </form>
                            <% } else { %>
                                Done
                            <% } %>
                        </td>
                    </tr>
                <% }) %>
            <% } %>
        </table>
    </div>

    <!-- Registered Users List -->
    <div class="card">
        <h3>Registered Users (<%= users.length %>)</h3>
        <table>
            <tr>
                <th>Phone Number</th>
                <th>User ID</th>
                <th>Balance</th>
                <th>Team Commission</th>
            </tr>
            <% users.forEach(u => { %>
                <tr>
                    <td><%= u.phone %></td>
                    <td><%= u.referral_code %></td>
                    <td><b><%= u.balance.toFixed(2) %> USDT</b></td>
                    <td><b><%= u.team_commission.toFixed(2) %> USDT</b></td>
                </tr>
            <% }) %>
        </table>
    </div>

</body>
</html>
